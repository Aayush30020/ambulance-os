name: Keep AmbulanceOS Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render backend
        run: |
          curl -f --max-time 30 https://ambulance-os-backend.onrender.com/ || exit 1
